<MudThemeProvider Theme="SpaceNavigateTheme" />

@code {
    MudTheme SpaceNavigateTheme = new MudTheme()
        {
            Palette = new Palette()
            {
                Primary = Colors.DeepPurple.Darken1,
                TextPrimary = Colors.Shades.White,
                TextDisabled = Colors.Grey.Darken3,
                LinesInputs = Colors.Grey.Default,
                TableStriped = Colors.Shades.Black,
                TextSecondary = Colors.Shades.White,
                ActionDefault = Colors.DeepPurple.Lighten1,
                ActionDisabled = Colors.Grey.Darken3,
                ActionDisabledBackground = Colors.Grey.Darken4,
                Error = Colors.Red.Darken4
            },


        };
}